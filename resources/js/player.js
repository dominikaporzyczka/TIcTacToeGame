/**
 * @description Constructor function to storage date
 * 
 * @param {string} color - color picked by player 
 * @param {string} symbol - circle or cross icon
 */
function Player(color, symbol) {
    this.color = color;
    this.symbol = symbol;
}